---
name: algorithm-tutorial-generator
description: 生成算法专题的总结、教程、学习笔记、tutorial
---

# Skill: 算法专题教程生成器

## 触发条件
当用户要求生成某个算法专题的总结、教程、学习笔记、tutorial 时，使用本 skill。
如果工作区里已有同类教程，但其结构、代码风格或排版与本 skill 冲突，**一律以本 skill 为准**，不要把那类文件当作风格参考。

---

## 工作流程

### Phase 1: 调研与规划
1. 使用 `web_search` 搜索该算法的经典题目、核心模型、解题模板
2. 收集 **10-20 道经典例题**，按难度分 4-5 级
3. 确定该算法有哪几种子模型（如背包的 0/1、完全、多重、分组、混合）

### Phase 2: 生成文档
文件名：`{topic}-dp-tutorial.md` 或 `{topic}-tutorial.md`
保存位置：用户工作目录（通常 `C:\Code\algorithm\`）
**行尾格式：必须使用 \r\n（Windows 换行）**

---

## 文档标准结构

```markdown
# {算法名称}（{英文/全称}）完全指南

## {emoji} 从入门到精通的渐进式讲解

---

## 目录
1. [什么是{算法名称}](#一什么是xxx)
2. [核心思想与前置知识](#二核心思想与前置知识)
3. [{子模型/分类详解}](#三xxx)
4. [N 道经典例题详解](#四n-道经典例题详解)
5. [常见技巧与思路总结](#五xxx)
6. [优化方法 / 解题思路框架](#六xxx)
7. [终极总结与归纳](#七终极总结与归纳)

## 一、什么是{算法名称}
## 二、核心思想与前置知识
## 三、子模型/分类详解（视算法而定）
## 四、N 道经典例题详解
## 五、常见技巧与思路总结
## 六、优化方法 / 解题思路框架
## 七、终极总结与归纳
```

### 各章节规范

**一、什么是{算法名称}**
- 2-3 段通俗解释
- ASCII art 图示（可选）
- 与相关算法的对比表格

**二、核心思想与前置知识**
- `### 2.1` / `### 2.2` / `### 2.3` 子节编号
- 2.x 通用框架代码：默认使用 **关键变量 + 关键函数 + 核心转移骨架** 的讲义风格，不写完整程序
- 仅当用户**明确要求完整模板/完整可运行程序**时，才允许 `#include`、`main()`
- 即使是骨架代码，也必须保留关键初始化、可达性判断、合法性判断和边界处理，不能省略到会误导读者
- 骨架代码里不得保留未使用变量
- 其余子节用文字 + 代码块 + 表格

**三、子模型详解**
- `### 3.1` / `### 3.2` 等
- 每个模型：简要说明 → `状态定义 / 转移关键 / 适用信号 / 实现要点`（```代码块```）→ 简洁模板代码（```cpp```）
- 若实现里使用“合法状态编号 id”而不是直接下标访问 bitmask，必须明确写出 `mask = states[id]` 一类映射说明
- 结尾可加模型对比总表

**四、例题详解** — 核心章节，格式最严格：

难度分级头（每级一个）：
```
### ═══════════════════════════════════════
### 🟢 入门级 Level 1：xxx
### ═══════════════════════════════════════
```
难度 emoji 级别：🟢(L1) 🟡(L2) 🟠(L3) 🔴(L4) 🟣(L5) 🔥(L6)

每道题格式：
```
### 题目 N：标题（来源平台 题号）

> 题意简述（1-3 行）

**分析**：简要分析思路（1-3 行）

\```
状态定义：dp[...] = ...
转移方程：dp[...] = ...
边界条件：dp[...] = ...
答案位置：dp[...] 或 max/min(...)
\```

**C++ 代码**：
\```cpp
// 仅核心代码，不含 #include / using namespace / int main()
// 声明问题特有的全局变量（dp数组、答案变量、必要的结构体）
// 核心函数体
// 最后一行可写调用方式或辅助函数说明，但不要在代码注释里重复“答案：...”
\```

**核心启示**：
- **加粗关键词** + 解释（2-4 条）
- 提炼可迁移的通用方法论

---
```

**五、技巧总结**
- `### 5.1` 到 `### 5.N`
- 每条技巧配简短代码或公式

**六、优化方法 / 解题框架**
- 可含 ASCII 流程图
- "信号词 → 题型" 判断表格

**七、终极总结与归纳**
```
### 7.1 {算法名称}的本质（一句话 + 展开）
### 7.2 核心要点（5-6 条编号列表）
### 7.3 难度分级路线图（代码块，Level 1-5/6，✅ 已覆盖题 / ▪ 拓展方向）
### 7.4 N题总览表
  | # | 题目 | 类型/模型 | 核心技巧 | 难度 |
  ⭐ 到 ⭐⭐⭐⭐⭐
### 7.5 口诀（引用块 + emoji，4-8 行押韵中文记忆口诀）
```

---

## 代码风格（最关键！）

### 总原则
- **正确性优先于篇幅、排版和炫技**
- 任何“简化骨架”的行为，都不能省掉关键 base case、不可达态处理、越界 guard、合法性判断
- 文中 `状态定义 / 转移方程 / 边界条件 / 答案位置` 必须与代码骨架一致，不能前后口径不同

### 例题代码 — 简洁风格
- **禁止**：`#include`、`using namespace std;`、`int main()`、`scanf`/`cin`、`return 0`
- **保留**：
  - 问题特有的全局变量：`const int MAXN = 305;`、`int dp[MAXN][MAXN];`、`int ans;`
  - 必要的结构体：`struct Edge { int to, w; };`
  - 核心函数（`void dfs(...)` / `void solve(...)` / 直接循环体）
- **注释职责**：只解释变量、辅助函数或关键语句的作用；不要写“答案：...”这类讲课提示
- **答案位置**：统一放在代码块外的“答案位置：...”一行说明，不在代码注释里重复
- 代码长度：通常 5-25 行
- 变量命名简洁：`sz`、`adj`、`dp`、`f`、`ans`
- 命名尽量前后一致：如 `states/st`、`id/pre_id`、`compat`、`last_offset`
- 禁止保留无意义或未使用变量（如教程骨架里定义了 `bits` 却完全不用）

### 模板/框架代码 — 精简骨架为主（section 二 默认）
- 默认不给完整程序，只给关键变量、关键函数与转移骨架
- 若用户明确要求完整模板，才允许 `#include <bits/stdc++.h>`、`using namespace std;`、`int main()`
- 即使给骨架，也要用 best practice：变量有意义、初始化清晰、guard 明确、风格统一

### 示例对比
```
❌ 错误（例题中不应出现）:
#include <bits/stdc++.h>
using namespace std;
int dp[305][305];
int main() {
    int n; cin >> n;
    ...
    cout << dp[1][n] << endl;
}

✅ 正确（例题代码风格）:
const int MAXN = 305;
int dp[MAXN][MAXN];

void solve(int n) {
    for (int len = 2; len <= n; len++) {
        for (int i = 1; i + len - 1 <= n; i++) {
            int j = i + len - 1;
            dp[i][j] = 0x3f3f3f3f;
            for (int k = i; k < j; k++){
                dp[i][j] = min(dp[i][j], dp[i][k] + dp[k+1][j] + pre[j] - pre[i-1]);
            }
        }
    }
}
// pre[i] = a[1]+...+a[i]; solve(n)
答案位置：dp[1][n]
```

---

## 排版风格

1. **全文中文**，代码/算法名可用英文
2. **章节编号**：一级用中文数字（一、二、三），二级用阿拉伯数字（2.1、3.2）
3. **分隔线**：题目之间用 `---`，难度级别用 `### ═══...═══` 三行装饰
4. **代码块**：状态定义用 ` ``` `（无语言标记），C++ 代码用 ` ```cpp `
5. **强调**：关键词用 `**加粗**`，核心公式用行内代码 `` `dp[i][j]` ``
6. **表格**：对齐，用于对比、总览、信号词映射等

---

## 内容原则

1. **渐进式**：从最简单到最复杂，后题复用前题思维
2. **正确性第一**：文字解释、状态定义和代码骨架必须一致，不能为了简洁牺牲准确性
3. **best practice**：骨架代码也应具备良好命名、无未使用变量、关键 guard 明确
4. **本 skill 优先**：若本地已有教程与本 skill 冲突，以本 skill 为最终标准
5. **核心启示要可迁移**：不是就题论题，而是提炼通用方法论
6. **图解辅助**：ASCII art 展示状态转移过程（尤其对区间 DP、树形 DP）
7. **口诀收尾**：帮助快速回忆整个知识体系，4-8 行押韵中文
8. **题目来源多样**：混合 LeetCode、洛谷、AcWing、CSES、经典竞赛题
9. **每题独立可读**：不依赖前文代码，每题的状态定义和代码自成一体
